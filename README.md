# CSV Dashboard Generator

A Streamlit app that lets you upload a CSV file, preview the data, and create simple charts.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

## Deploy publicly

Upload this project to GitHub and use Streamlit Community Cloud to deploy it publicly.
